import { PropsWithChildren, ReactNode } from 'react'



const Section = ({ children }: { children: ReactNode } , props : PropsWithChildren) => {
    return (
        <>
        <section></section>
        </>
    )
}